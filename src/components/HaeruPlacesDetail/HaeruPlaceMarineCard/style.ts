import styled from 'styled-components';

export const HaeruPlaceMarineCardWrapStyle = styled.article`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10px;

  & > span {
    font-size: 14px;
  }
`;
